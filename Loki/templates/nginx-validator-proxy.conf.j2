server {
    listen {{ proxy_listen_port }};
    server_name _;

    location / {
        proxy_pass http://{{ proxy_upstream_ip }}:{{ proxy_upstream_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 30s;
    }
}
